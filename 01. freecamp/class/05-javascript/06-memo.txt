조건문
true, false로 컴퓨터가 판단하고, 이 판단을 근거로 다른 엑션을 실행하게 한다.

조건이 맞으면 A실행, 맞지 않으면 B실행

if(조건) {
    A 실행
} else {
    B 실행
}

if(A !==B) {
    명령문 1
} eles if((C===D) && (E===F)) {
    명령문 2
} else {
    명령문 3
}